---
import { buildStyleShowcaseTheme } from '../../../../lib/style-showcase-theme';

const { style } = Astro.props;
const title = style && style.title ? style.title : 'UI Preview';
const theme = buildStyleShowcaseTheme(style ?? { slug: 'default-showcase', raw: {} });
---

<div class={`rounded-[2rem] border p-6 ${theme.motionClass}`} style="border-color:var(--ss-border);background:var(--ss-panel);">
  <p class="text-xs font-semibold uppercase tracking-[0.18em]" style="color:var(--ss-muted);">Device Frame</p>
  <div class="mt-4 rounded-[1.75rem] border p-4" style="border-color:var(--ss-border);background:var(--ss-panel-soft);">
    <div class="rounded-[1.25rem] p-4 shadow-sm" style="background:var(--ss-panel);box-shadow:0 10px 30px -24px var(--ss-accent);">
      <p class="text-xs font-semibold uppercase tracking-[0.18em]" style="color:var(--ss-muted);">{title}</p>
      <div class="mt-3 h-24 rounded-xl" style="background:linear-gradient(135deg,var(--ss-accent),var(--ss-accent-2));"></div>
      <div class="mt-3 grid gap-2">
        <div class="h-3 rounded-full" style="background:var(--ss-bg-to);"></div>
        <div class="h-3 rounded-full" style="background:var(--ss-bg-to);"></div>
        <div class="h-3 rounded-full" style="background:var(--ss-bg-to);"></div>
      </div>
    </div>
  </div>
</div>
