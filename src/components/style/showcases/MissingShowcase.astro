---
const { style } = Astro.props;
const title = style && style.title ? style.title : 'Unknown';
---

<div class="rounded-[2rem] border border-dashed border-slate-200 bg-slate-50 p-6">
  <p class="text-sm font-semibold text-slate-700">尚未建立展示元件：{title}</p>
</div>
